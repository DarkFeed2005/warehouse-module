# warehouse-module
The full module under deliverables/ — sql/schema.sql (schema + seed), backend/ (Express + JWT + role guards + zod + transactional intake), and frontend/ (Next.js 14 App Router + Tailwind dark-green/cream theme with sidebar, KPI gradients, warehouse cards, detail page with div-bar chart, intake form with live capacity projection, and alerts panel 
